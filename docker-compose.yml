version: '3'
services:

  #APP SERVICES
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: portofolio_web
    restart: always
    working_dir: /app
    ports:
      - "9000:9876"
